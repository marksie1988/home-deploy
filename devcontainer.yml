
- name: Create Dev Server
  hosts: devcontainer
  become: true
  tasks:
  - name: Install podman suite of tools
    ansible.builtin.package:
      name: "{{ item }}"
      update_cache: yes
    with_items:
    - podman
    - podman-docker
